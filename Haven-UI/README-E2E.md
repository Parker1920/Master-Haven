Playwright E2E tests

- Install dependencies: npm install
- Install browsers for Playwright: npm run e2e:install
- Run dev server: npm run dev
- Run tests: npm run e2e

Note: Ensure the backend server API (FastAPI) is running and accessible at http://localhost:8000 while running the UI dev server on http://localhost:5173
