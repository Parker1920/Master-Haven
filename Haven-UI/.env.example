# ===========================================
# Haven UI Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# DO NOT commit the .env file to version control!

# -----------------
# ADMIN & AUTH
# -----------------

# Admin password for web UI login (required for admin actions)
HAVEN_ADMIN_PASSWORD=change_this_password

# Optional admin token for header-based admin access (legacy)
HAVEN_ADMIN_TOKEN=

# API key for Discord bot integration
# The bot should send this in the X-API-Key header
HAVEN_API_KEY=your_api_key_here

# -----------------
# PATH OVERRIDE
# -----------------

# Optional: Override the UI's data, logs, and dist folder location
# If not set, defaults to the Haven-UI folder in the repository
# HAVEN_UI_DIR=/path/to/Haven-UI
