"""Haven smoke + verification — utility modules.

This package holds the alerter and redaction helpers used by run_smoke.sh
and run_verify.sh. Kept under tests/ so the suite is fully self-contained.
"""
